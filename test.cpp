#include "stdio.h"
#include "VisualEffects.h"
#include <vector>

int main() {
/*  std::vector<FadingEffectType*> effect;
  effect.resize(2);
  effect[0] = new FlippedFieldEffect;
  effect[1] = new SemicubesEffect;
  printf("%d\n", effect[0]->duration());
  printf("%d\n", effect[1]->duration());
//   printf("%d\n", FlippedFieldEffect::duration);
//   printf("%d\n", SemicubesEffect::duration);
  delete effect[0];
  delete effect[1];*/
  return 0;
}
